node_modules
dist
.next
out
build
coverage
.turbo
pnpm-lock.yaml
*.md.backup

# Documentación autorizada manualmente (no tocar formato)
docs/**/*.md
prompts/**/*.md
!docs/adrs/**/*.md

# UI kit aportado por el usuario — material de referencia, no código del proyecto.
docs/ui-kit/**

# Evidencia descargada de runs de CI (reportes Playwright, etc.). Son
# artefactos generados, no deberían pasar por el formatter.
docs/evidence/**
